use crate::config;
use crate::processor::{Image, Process};

struct Format {}

impl Format {
    pub fn new(format_opt: config::Format, quality_opt: config::Quality) -> Format {
        todo!();
    }
}
